<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Ticket</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f4f4f4;
      }
      .ticket-container {
        background: white;
        border-radius: 12px;
        width: 600px;
        padding: 20px;
        text-align: center;
        box-sizing: border-box;
        position: relative;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      .logo {
        height: 15%;
        display: flex;
        justify-content: center;
        align-items: center;
        margin-bottom: 10px;
      }
      .logo img {
        max-height: 100%;
      }
      .event-name {
        background: linear-gradient(90deg, #00c6ff, #0072ff);
        color: white;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 12px 12px 12px 12px;
      }
      .event-name h1 {
        margin: 0;
        font-size: 28px;
        font-weight: 700;
        padding: 20px;
      }
      .ticket-details-table {
        width: 100%;
        margin-top: 20px;
        display: flex;
        justify-content: center;
      }
      .ticket-details-table table {
        width: 80%;
        border-collapse: collapse;
        margin: 0 auto;
      }
      .ticket-details-table td {
        padding: 10px;
        font-size: 18px;
        font-weight: 700;
        border-bottom: 1px solid #ddd;
      }
      .ticket-details-table td:first-child {
        text-align: left;
        width: 40%;
      }
      .ticket-details-table td.dynamic-content {
        text-align: left;
        width: 60%;
      }
      .qr-code img {
        min-height: 300px;
        min-width: 300px;
      }
      .footer {
        margin-top: 10px;
        font-size: 18px;
        color: #555;
        text-align: center;
        margin-bottom: 10px;
      }
      .download-button {
        margin-top: 10px;
        padding: 10px 20px;
        background-color: #0072ff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 18px;
        font-weight: 700;
      }
      @media print {
        .download-button {
          display: none;
        }
      }
      @media (max-width: 600px) {
        body {
          height: auto;
          padding: 10px;
        }
        .ticket-container {
          width: 100%;
          padding: 10px;
        }
        .event-name h1 {
          font-size: 20px;
        }
        .ticket-details-table td {
          font-size: 14px;
        }
        .qr-code img {
          width: 200px;
          height: 200px;
        }
        .download-button {
          font-size: 16px;
          padding: 8px 16px;
        }
      }
    </style>
  </head>
  <body>
    <div class="ticket-container">
      <div class="logo">
        <% if (ticket.logoUrl) { %>
        <img src="<%= ticket.logoUrl %>" alt="Logo" />
        <% } %>
      </div>
      <div class="event-name">
        <h1><%= ticket.eventName %></h1>
      </div>

      <div class="qr-code">
        <% if (ticket.qrCodeUrl) { %>
        <img src="<%= ticket.qrCodeUrl %>" alt="QR Code" />
        <% } %>
      </div>

      <div class="footer">
        <strong>Booking ID:</strong>
        <span class="dynamic-content"><%= ticket.Booking_id %></span>
      </div>

      <div class="ticket-details-table">
        <table>
          <tr>
            <td><strong>Ticket Name:</strong></td>
            <td class="dynamic-content"><%= ticket.TicketName %></td>
          </tr>
          <tr>
            <td><strong>Ticket Type:</strong></td>
            <td class="dynamic-content"><%= ticket.BookedEventTicketType %></td>
          </tr>
          <tr>
            <td><strong>Ticket Quantity:</strong></td>
            <td class="dynamic-content"><%= ticket.TicketQuantity %></td>
          </tr>
          <tr>
            <td><strong>Date:</strong></td>
            <td class="dynamic-content"><%= ticket.EventDate %></td>
          </tr>
          <tr>
            <td><strong>Time:</strong></td>
            <td class="dynamic-content"><%= ticket.EventTime %></td>
          </tr>
          <tr>
            <td><strong>Venue:</strong></td>
            <td class="dynamic-content"><%= ticket.EventVenue %></td>
          </tr>
          <tr>
            <td><strong>City:</strong></td>
            <td class="dynamic-content"><%= ticket.VenueCity %></td>
          </tr>
        </table>
      </div>

      <button class="download-button" onclick="downloadPDF()">
        Download Ticket
      </button>
    </div>

    <script>
      function downloadPDF() {
        window.location.href =
          "/eventstickets/download-ticket-pdf/<%= ticket.Booking_id %>";
      }
    </script>
  </body>
</html>
